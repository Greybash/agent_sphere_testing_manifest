apiVersion: v1
kind: Namespace
metadata:
  name: imagepull-secret-test
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: private-app
  namespace: imagepull-secret-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: private-app
  template:
    metadata:
      labels:
        app: private-app
    spec:
      containers:
        - name: app
          image: myprivateregistry.io/myapp:latest  # ❌ private image
          ports:
            - containerPort: 80